/* eslint-disable @typescript-eslint/ban-ts-comment */
import React from 'react';

// @ts-ignore:
export const appContext = React.createContext<any>({} as any);

export const { Provider } = appContext;
